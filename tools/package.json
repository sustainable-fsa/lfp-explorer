{
  "name": "fsa-normal-grazing-period-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only audit harness for the FSA Normal Grazing Periods web map. Nothing here ships: the app itself is a build-step-free static page with zero runtime dependencies, and these packages exist only to drive it in CI and locally.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "a11y": "node a11y-audit.mjs",
    "verify": "node verify.mjs",
    "html": "html-validate ../index.html",
    "lhci": "lhci autorun --config=../.lighthouserc.json"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@lhci/cli": "^0.15.1",
    "html-validate": "^11.8.0",
    "playwright": "^1.62.1"
  }
}
